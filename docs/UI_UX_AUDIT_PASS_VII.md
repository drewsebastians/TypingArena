# UI/UX Audit — Pass VII (Independent, Evidence-Gated)

**Date:** 2026-08-26
**Baseline SHA:** `9109fe824ca8b241f3d22129f094f0ee15c51ccd`
**Branch:** `typingarena-ui-ux-polish` (before implementation)
**Viewports:** desktop 1440×900, 1280×800; tablet 768×1024; mobile 390×844 / 375×667 / 320×568
**Evidence:** `artifacts/ui-ux/before/` (24 screenshots, 12 routes × 2 viewports)
**Method:** browser screenshots + task-based journeys (A–I) + source audit + Playwright checks

## Seed hypotheses (from prompt §27)

| ID | Hypothesis | Verdict |
|---|---|---|
| H1 | Navigation overload — 15 top-level destinations hurt discovery | **CONFIRMED** — see UX-001, UX-002 |
| H2 | Homepage feature-catalog effect (19 cards) | **CONFIRMED** — see UX-003 |
| H3 | Font inconsistency Geist vs Arial Helvetica | **CONFIRMED** — see UX-004 |
| H4 | Locale semantics — html lang="en" static while UI is bilingual | **CONFIRMED** — see UX-005 |
| H5 | Locale update scope — toggle doesn't reactively update all surfaces | **CONFIRMED** — see UX-006 |
| H6 | Public backend error language "See README" developer-facing | **CONFIRMED** — see UX-007 |
| H7 | Mobile chip navigation at 320–390 px | **CONFIRMED** — see UX-002 |
| H8 | Technical trust messaging density | **CONFIRMED** but LOW severity — see UX-012 |

---

## Inventory

| ID | Surface | Viewport | Task | Issue | Evidence | Severity | User impact | Proposed direction | Confidence |
|---|---|---|---|---|---|---|---|---|---|
| UX-001 | Global nav (Header) | Desktop 1440, tablet 768 | A, F, G, I — discover Tools | 15 undifferentiated pills in one row; related destinations (Sprint/Dictation/Transcription vs Daily/Leaderboard/Seasons/Friends vs Teams/Assessments/Career) have no grouping; competitor distinction not obvious; new roadmap items added linearly | `before/home-desktop-1440x900.png` header row wraps/overflows; xl breakpoint hides all but logo; cognitive load | **P0** | New and competitive users cannot map product model; key tools hidden among parity items | Group into 4 primaries (Practice / Compete / Teams / Progress) with progressive disclosure (desktop dropdown, mobile drawer); keep all routes reachable; measure time-to-find | **HIGH** |
| UX-002 | Mobile nav | 320×568, 390×844 | A, B — mobile first run | Horizontal scrollable pill row; items off-screen have zero affordance; accidental horizontal drag conflicts with vertical scroll; active pill can be hidden; 14 items × 320 px requires ~3 swipes; touch targets ~28 px high < 44 px | `before/home-mobile-390x844.png` header second row overflow | **P0** | Mobile users miss core tools; fails WCAG touch target | Replace with accessible drawer (hamburger) with grouped sections; keyboard/focus/Escape managed; current route highlighted | **HIGH** |
| UX-003 | Homepage discovery grid | Desktop + mobile | A — what can I do? | 19 tool cards in one undifferentiated grid (sm:grid-cols-3) duplicate primary nav, push "How it works" below fold; no hierarchy among practice vs compete vs groups; labels like "Library" ambiguous | `before/home-desktop-1440x900.png` cards 4–19 below fold | **P1** | Overwhelm delays first action; SEO internal links diluted | Group cards by category (Practice / Compete / Create & Work) with section headings + progressive disclosure (show 3–4 primary, "Explore all" expands); keep links as <a> | **HIGH** |
| UX-004 | Typography | All | B, C — reading/metrics | `layout.tsx` loads Geist/Geist_Mono via next/font (CSS variables), but `globals.css` body rule forces `Arial, Helvetica, sans-serif`; Geist never wins for body text; typing content, headings, and metric numbers use different stacks; digit alignment varies | Source: `globals.css:18` vs `layout.tsx:8`; rendered computed font = Arial in browser devtools | **P1** | Inconsistent reading rhythm; metric columns misalign; wastes loaded font payload | Define coherent font system: body = Geist Sans via var, mono metrics/typing = Geist Mono; remove Arial override; keep typing area highly readable (1.6 line-height, mono) | **HIGH** |
| UX-005 | HTML lang | All | I — Indonesian user + a11y | Root `<html lang="en">` is static; switching UI to `id` leaves document language incorrect; screen readers mispronounce Bahasa Indonesia; fails WCAG 3.1.1 | `layout.tsx:36` `lang="en"` ; `i18n.ts` persists locale but never touches document | **P1** | Accessibility/semantic defect; AT users affected | Update `document.documentElement.lang` reactively when locale changes (effect in Header or layout client wrapper); keep static export safe (initial en, then client effect) | **HIGH** |
| UX-006 | Locale reactivity | All | I — switch language | `Header.flipLocale` updates header-local state + localStorage but other components call `t(key)` reading `getLocale()` imperatively without subscription; they only re-render on pathname change (useEffect dep). Panels like Career/Daily/Leaderboard stay in previous language until navigation | Source `Header.tsx:38-42` vs `CareerPanel` `t("career.subtitle")` with no locale dep | **P1** | Bilingual promise broken; user thinks toggle is buggy | Introduce lightweight locale subscription (storage event + custom event) or context; ensure t() consumers re-render; persist across reload | **MEDIUM** |
| UX-007 | Backend-degraded copy | Leaderboard, Daily, Friends, Teams, Assessments | E, G, H — no backend demo | `i18n.ts:48 common.backendRequired = "See README → Shared competition setup"` — developer-facing text shown to ordinary users where honest user-facing copy needed | `before/leaderboard-desktop-1440x900.png` (setup notice) | **P1** | Public demo looks unfinished/developer-only; erodes trust | Replace with user-facing state: what feature does, why backend needed, what works offline; keep truth, remove repo instructions from UI | **HIGH** |
| UX-008 | Result hierarchy | Typing/dictation result | B, C — how did I do? | ResultCard + per-mode results show primary metrics (WPM/accuracy), heatmap, correction latency, bigram errors, replay ratio all at equal visual weight; technical jargon (typed-scope accuracy, bigram) competes with "try again" CTA | `before/typing-pre-test-desktop...` result state (when available) | **P1** | New users cannot parse success vs next step | Establish hierarchy: large primary metric, secondary accuracy, CTA row, then progressive disclosure for advanced analytics ("Details") | **MEDIUM** |
| UX-009 | Active-exercise focus | Typing/Dictation active | B, C — type without distraction | Header, footer, ad slots, mode pickers remain at full prominence during active test; no focus state; long instructions compete with stream | Browser evidence: typing active shows header + HUD + stream equally weighted | **P1** | Cognitive competition reduces performance/immersion | Dim header/footer during active state (reduced opacity / compact), reserve but mute secondary chrome; keep accessibility controls | **MEDIUM** |
| UX-010 | Visual system drift | Teams, Assessments, Career, Custom | G, H | Rapid roadmap expansion produced divergent card borders, pill styles, button hierarchies (some use `rounded-full bg-black`, others border), inconsistent empty-state wording, spacing scale 4–10 mix | Source diff across Panels | **P2** | Product feels stitched; maintenance cost | Extract lightweight primitives (PageHeading, SectionCard, PrimaryButton) where reuse measurably improves consistency; tokenize radius/spacing | **MEDIUM** |
| UX-011 | Responsive overflow | Leaderboard, Seasons, Teams assignments | E, G — mobile table | Leaderboard/seasons tables, career result grids, team assignment rows can overflow at 320 px; horizontal scroll not indicated; sticky header interaction | Expected at 320 stress | **P1** | Clipped controls; WCAG reflow failure | Wrap/stack metric cards at <640 px; ensure tables scroll with affordance; verify touch targets ≥44 px | **MEDIUM** |
| UX-012 | Trust messaging density | Homepage hero | A — value prop | Hero already states value; "Deterministic scoring • no AI at runtime" pill + footer "Scoring v2.0.0" + large "How it works" block together create high technical density before user has tried product | `before/home-desktop-1440x900.png` | **P2** | Delays emotional connection | Keep one trust pill in hero, move detailed scoring provenance to footer/supporting context; preserve claims, reduce prominence | **MEDIUM** |
| UX-013 | Dark mode continuity | All | General | `globals.css` body bg uses light `#ffffff` regardless of `dark:` Tailwind classes; `.ad-slot` dark variant exists but body/header/footer dark continuity needs verify; heatmap/result colors vs dark bg | Source: `--background: #ffffff` not theme-aware | **P2** | White flash/incorrect card bg in dark | Ensure dark `background` token + header/footer dark classes produce continuous dark page; verify ad placeholder contrast | **MEDIUM** |
| UX-014 | Empty/loading/degraded states | Teams, Custom, Assessments | G, H | "Nothing yet" vs "No assignments yet" vs setup notices use different tones; loading states are bare text; permission errors generic | Source panels | **P2** | Inconsistent mental model | Standardize EmptyState component; loading spinner/skeleton; permission copy specific | **MEDIUM** |

## Confidence gate

Only HIGH/MEDIUM-confidence findings may drive code changes. P3 omitted. P2 included only where they form a coherent system with P0/P1.
